.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:plivoapps, :index)
      %li.active=link_to pat(:new), url(:plivoapps, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:plivoapp)
    .inner
      -form_for :plivoapp, url(:plivoapps, :create), :class => :form do |f|
        =partial "plivoapps/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
